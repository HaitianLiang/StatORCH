# Results

- `reported/`: structured values already reported in the current manuscript/materials. These are provenance-controlled presentation inputs, not raw per-unit reruns.
- `demo/`: outputs generated by `statorch demo` from the self-contained synthetic scenario.

Never combine these two sources in a manuscript table without explicit labeling.
