# StatOrch frontend

No build step and no npm dependency.

```bash
python ../scripts/serve_frontend.py --port 8000
```

The app reads JSON from `data/`. Regenerate it with `python ../scripts/export_frontend_data.py`.
